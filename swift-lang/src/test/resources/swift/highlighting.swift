/// 🧭 Documentation comment
import Foundation

var counter = 0
let ratio = 0.1
let name: String? = nil

func run(items: [Int]) {
    for item in items {
        if item > 0 {
            counter += item
        }
    }
}
